meta {
  name: create one
  type: http
  seq: 1
}

post {
  url: {{base_url}}/technologies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "label": "Java"
  }
}
